apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Flux system first
  - flux-system
  # Then core infrastructure
  - kyverno
  - ingress-nginx
  - kube-prometheus-stack
  # Policies after the tools are installed
  - kyverno-policies
  # Finally tenants
  - tenants.yaml
